defaults:
  - config
  - override scenario: stacked_middle_target   # optional: pass scenario=<name> to use a preset environment
  - _self_

# Environment

planner:
  target_prob: 0.4

# Runtime
pause_before_verify: false